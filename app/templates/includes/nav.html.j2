<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Pokemon</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="navbar-nav">
                {% if current_user.is_authenticated %}
                <a class="nav-link" href="{{url_for('main.index')}}">Home</a>
                <a class="nav-link" href="{{url_for('main.lookup')}}">Catch</a>
                <a class="nav-link" href="{{url_for('build.my_pokemon')}}">My Cards</a>
                <a class="nav-link" href="{{url_for('build.battle')}}">Battle</a>
                {% else %}
                <a class="nav-link" href="{{url_for('auth.login')}}">Login</a>
                <a class="nav-link" href="{{url_for('auth.register')}}">Register</a>
                {% endif %}
            </div>
                {% if current_user.is_authenticated %}
                <ul class="navbar-nav ms-auto align-items-center">
                <a href="{{url_for('build.user_info')}}">
                    <img src="{{current_user.get_icon_url()}}" style="height:50px;">
                </a>
                <li class="nav-item">
                <div class="row">
                <div class="col-12">    
                    <a class="nav-link" href="{{url_for('auth.edit_profile')}}"><i class="bi bi-file-earmark-person-fill"></i></a>
                </div>
                <div class="col-12">
                    <a class="nav-link" href="{{url_for('auth.logout')}}"><i class="bi bi-door-open-fill"></i></a>
                </div>
                </div>
                
                </li>
                </ul>
                {% endif %}

        </div>
    </div>
    </nav>